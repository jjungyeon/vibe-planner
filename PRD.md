# 프로젝트 PRD

> 「생성형 AI 기반 바이브코딩 활용 입문·실전」 06차시 경량 PRD 템플릿 기준
> 작성일: 2026-08-11 / 작성자: 황정연

---

## 1. 프로젝트 이름

**(미정)** — 서비스 명칭 확정 후 기입. 임시 표기는 `vibe-planner`

---

## 2. 한 줄 요약

만들고 싶은 것을 질문 6~7개로 답하면, 필요한 화면 목록과 화면 연결, 그리고 바이브코딩 도구에 그대로 붙여넣을 개발 프롬프트를 만들어주는 웹사이트.

---

## 3. 배경과 문제

### 현재 상황
바이브코딩 강의를 들은 비전공자는 도구 사용법은 익혔지만, 자기 프로젝트를 시작하는 첫 지점에서 멈춘다. 코드를 쓰는 방법이 아니라 **무엇을 만들지를 정하는 방법**을 모르기 때문이다.

### 사용자 문제
> 비전공자 수강생은 만들고 싶은 것이 있어도 무엇을 어떤 순서로 정해야 하는지 몰라 개발을 시작하지 못하고, 특히 화면을 몇 개 만들지와 화면끼리 어떻게 잇는지에서 가장 크게 막힌다.
>
> 이 프로젝트는 PRD 항목을 질문으로 바꿔 답하는 동안 개념까지 익히게 하고, 그 답을 화면 구성안과 개발 프롬프트로 변환한다.

### 기존 대안의 한계

| 대안 | 한계 |
|---|---|
| PRD 템플릿 문서 (.md) | 빈칸 13개를 혼자 채워야 함. 각 항목이 무슨 뜻인지 모름 |
| AI에게 직접 물어보기 | 매번 다른 답. 화면 개수가 과도하게 늘어남 |
| 강의 교안 다시 보기 | 개념은 있으나 내 프로젝트에 적용하는 단계가 비어 있음 |

---

## 4. 타겟 사용자

| 항목 | 내용 |
|---|---|
| **주요 사용자** | 「생성형 AI 기반 바이브코딩 활용 입문·실전」 수강생. 비전공자, 개발 경험 없음 |
| **디지털 수준** | AI 챗봇 사용은 익숙함. Claude Code·VS Code·Vercel은 강의에서 처음 접함. PRD는 모르거나 한 번 들은 수준 |
| **사용 상황** | 장소 무관, PC 환경. 웹앱을 만들기로 마음먹은 직후, 코드를 한 줄도 쓰기 전 |
| **제약** | 전문 용어는 설명 없이 못 읽음. 4시간 안에 결과가 나와야 함. 이탈이 빠름 |

**페르소나는 과장하지 않는다.** 나이·직업·성격 등 근거 없는 설정은 넣지 않는다.

---

## 5. 사용자 가치

- 빈 문서 앞에서 멈추지 않는다
- 만들어야 할 화면의 개수와 연결이 눈에 보인다
- 개발 도구에 붙여넣을 문장이 손에 들어온다
- 답하는 동안 PRD가 무엇인지 알게 된다

---

## 6. 사용자 스토리

**US-01**
비전공자 수강생으로서 **내가 만들려는 것에 대해 무엇을 정해야 하는지 질문으로 안내받고** 싶다.
그래서 빈 문서 앞에서 멈추지 않고 기획을 끝낼 수 있다.

**US-02**
비전공자 수강생으로서 **내 답변이 몇 개의 화면으로, 어떻게 연결되는지 보고** 싶다.
그래서 무엇을 만들어야 하는지 이해한 상태에서 개발을 시작할 수 있다.

**US-03**
비전공자 수강생으로서 **개발 도구에 그대로 붙여넣을 프롬프트를 받고** 싶다.
그래서 기획과 개발 사이에서 다시 헤매지 않는다.

---

## 7. 기능 요구사항

| ID | 요구사항 | 연결 | 우선순위 | 수용 기준 |
|---|---|---|---|---|
| FN-01 | 사용자는 질문에 순서대로 답한다 | US-01 | Must | AC-01 |
| FN-02 | 각 질문 옆에 해당 PRD 항목명과 필요한 이유가 표시된다 | US-01 | Must | AC-02 |
| FN-03 | 모든 질문에 "잘 모르겠어요"가 있고, 선택 시 기본값이 적용된다 | US-01 | Must | AC-06 |
| FN-04 | 첫 화면에서 예시 카드를 골라 답이 채워진 상태로 시작할 수 있다 | US-01 | Must | AC-07 |
| FN-05 | 답변을 바탕으로 화면 목록과 화면 간 연결이 생성된다 | US-02 | Must | AC-03 |
| FN-06 | 답변과 화면 구성안을 합친 개발 프롬프트가 생성된다 | US-03 | Must | AC-04 |
| FN-07 | 생성된 프롬프트를 한 번에 복사한다 | US-03 | Must | AC-05 |
| FN-08 | 답변이 브라우저에 자동 저장되어 새로고침 후에도 유지된다 | US-01 | Should | AC-08 |
| FN-12 | 이미 계획이 선 사용자는 줄글을 적어 곧바로 개발 프롬프트를 받는다 | US-03 | Should | AC-09 |
| FN-09 | 답변 내용이 진행 중에 미리보기로 표시된다 | US-01 | Could | — |
| FN-10 | 결과물을 .md 파일로 내려받는다 | US-03 | Could | — |
| FN-11 | 화면 구성안을 도식으로 확인한다 | US-02 | Won't now | — |

**Must 7개.** 이 중 FN-02·FN-03·FN-07은 데이터 추가와 버튼 수준이며, 실제 개발 무게는 FN-01·FN-05·FN-06 세 개에 집중된다.

---

## 8. 비기능 요구사항

| ID | 분야 | 요구사항 | 확인 방법 |
|---|---|---|---|
| NF-01 | 사용성 | PRD를 모르는 사람이 사전 설명 없이 첫 질문에 답할 수 있다 | 강의 동기 1명에게 설명 없이 열어보게 함 |
| NF-02 | 사용성 | 첫 화면 진입부터 결과 확인까지 5분 이내 | 타이머로 직접 측정 |
| NF-03 | 사용성 | 화면에 A/B/C 같은 분류 기호나 개발 용어를 노출하지 않는다 | 전체 문구 검토 |
| NF-04 | 성능 | 결과 생성이 즉시 반응한다 | 규칙 기반 계산이므로 서버 왕복 없음 |
| NF-05 | 신뢰성 | 새로고침해도 입력한 답변이 남아 있다 | 새로고침 후 확인 |
| NF-06 | 보안 | 개인정보를 수집하지 않는다 | 입력 항목에 이름·연락처 없음 |
| NF-07 | 호환성 | 최신 Chrome·Edge PC 화면에서 정상 동작 | 두 브라우저에서 확인 |

---

## 9. 사용자 흐름

```
첫 화면 — 진입로 3개
│
├─ [만들고 싶은 게 있어요]  ──────────────────┐
│                                             │
├─ [이미 정해졌어요, 바로 쓸게요]              │
│   └─ 빠른 입력 화면 (한 화면)                │
│        · 줄글 입력 (만들려는 것을 자유롭게)  │
│        · 안 만들 것 입력 (선택)              │
│        └─ 생성 → 프롬프트 결과 화면          │
│                                             │
└─ [예시부터 볼래요]                          │
     └─ 예시 카드 8개                         │
          └─ 카드 선택 → 결과 즉시 표시        │
               └─ [내 걸로 바꾸기] ───────────┤
                                              ↓
                                       질문 1  무엇을 다루나요? (자유 입력)
                                       질문 2  누가 쓰나요?
                                       질문 3  비슷한 걸 골라주세요
                                       질문 4~7  갈래별 후속 질문
                                              ↓
                                       결과 화면
                                       · 화면 목록
                                       · 화면 간 연결
                                       · 기본값으로 정해진 항목 안내
                                       · 개발 프롬프트 + 복사 버튼
```

**세 경로는 서로 다른 사용자를 맡는다.**

| 경로 | 대상 | 결과물 |
|---|---|---|
| 질문 7개 | 무엇을 만들지 흐릿한 사람 | 화면 구성안 + 개발 프롬프트 |
| 줄글 (빠른 입력) | 이미 그림이 선 사람 | 개발 프롬프트 (통제 조건 포함) |
| 예시 카드 | 시작 자체를 못 하는 사람 | 완성 예시 → 수정 진입 |

**줄글 경로에는 화면 구성안이 없다.** 갈래 판정도 화면 연결도 거치지 않는다. 이 경로를 쓰는 사용자는 이미 화면 그림이 있으므로 US-02가 필요하지 않다.

---

## 10. 제외 범위 (Out of Scope)

| 제외 항목 | 이유 |
|---|---|
| 로그인·회원가입·서버 저장 | 4시간 범위 초과. 브라우저 저장으로 대체 |
| AI 호출 | 결과 일관성 확보와 시연 안정성을 위해 규칙 기반 선택 |
| 실제 코드 생성 | 이 서비스는 프롬프트까지만 만든다. 코드는 사용자가 바이브코딩 도구에서 만든다 |
| 아이디어 발굴 | 만들 것을 찾아주는 서비스가 아니다. 예시 갤러리는 시작 보조 장치일 뿐 |
| 화면 도식 그리기 | 다이어그램 라이브러리 도입 필요. 글 목록으로도 가치 전달됨 |
| 기획서 공유·협업 | 개인 도구로 한정 |
| 모바일 반응형 | PC 전용. 2단 레이아웃 유지를 위한 결정 |
| 다국어 | 한국어만 |
| 게임·실시간 채팅·지도 기반·파일 업로드형 서비스 | 세 갈래 규칙으로 커버되지 않음 |

**제외 범위는 포기가 아니라 이번 버전의 집중이다.** 로그인·서버 저장·AI 이름 생성·화면 도식은 다음 버전 후보 목록이다.

---

## 11. 데이터 구조

### 11-1. 질문지

#### 공통 질문

| ID | 질문 | 입력 형태 | PRD 항목 | 옆에 붙는 설명 |
|---|---|---|---|---|
| Q1 | 무엇을 다루는 서비스인가요? | 자유 입력 | 프로젝트 이름 | 여기 적은 말이 모든 화면 이름에 들어갑니다 |
| Q2 | 누가 쓰나요? | 나만 / 우리 팀·가족 / 손님·외부인 | 타겟 사용자 | 쓰는 사람이 정해져야 무엇을 뺄지 정해집니다 |
| Q3 | 만들려는 것과 가장 비슷한 걸 골라주세요 | 아래 3개 | 사용자 흐름 | 이 선택이 화면 구성을 결정합니다 |

**Q3 선택지 (화면 문구 / 내부 코드)**

- 🗂 가계부처럼 — 뭔가를 계속 적어서 쌓아둡니다 → `A` 기록형
- 🧮 BMI 계산기처럼 — 입력하면 답이 나옵니다 → `B` 변환형
- 📄 가게 소개 페이지처럼 — 정해진 내용을 보여줍니다 → `C` 안내형

#### A 기록형 후속 질문

| ID | 질문 | 선택지 | 결과 | 모르겠음 기본값 |
|---|---|---|---|---|
| A1 | 항목 하나마다 자세히 볼 내용이 있나요? | 있다 / 목록에 다 보인다 | 있다 → 상세 화면 | 켠다 |
| A2 | 사용자가 직접 등록하나요? | 직접 등록 / 미리 넣어둔 것만 본다 | 직접 등록 → 입력 폼 | 켠다 |
| A3 | 쌓인 것을 요약해서 봐야 하나요? | 필요하다 / 목록이면 충분하다 | 필요하다 → 요약 화면 | 끈다 |
| A4 | 항목이 수십 개 이상 쌓일까요? | 그렇다 / 적다 | 그렇다 → 검색·필터 | 끈다 |

#### B 변환형 후속 질문

| ID | 질문 | 선택지 | 결과 | 모르겠음 기본값 |
|---|---|---|---|---|
| B1 | 결과를 내려면 몇 가지를 물어봐야 하나요? | 1~3개 / 4개 이상 | 1~3개 → 단일 입력 화면 · 4개 이상 → 단계별 질문 화면 | 단계별 |
| B2 | 결과에 설명이나 근거가 붙나요? | 붙는다 / 값만 나온다 | 붙는다 → 결과 화면에 설명 영역 | 켠다 |
| B3 | 지난 결과를 다시 볼 수 있어야 하나요? | 필요하다 / 필요 없다 | 필요하다 → 기록 목록 화면 | 끈다 |

#### C 안내형 후속 질문

| ID | 질문 | 선택지 | 결과 | 모르겠음 기본값 |
|---|---|---|---|---|
| C1 | 보여줄 내용이 몇 덩어리인가요? | 1~4개 / 5개 이상 | 5개 이상 → 하위 페이지 분리 | 한 페이지 |
| C2 | 나열해서 보여줄 것이 있나요? | 있다 / 없다 | 있다 → 목록 섹션 | 켠다 |
| C3 | 연락받을 창구가 필요한가요? | 필요하다 / 필요 없다 | 필요하다 → 문의 폼 | 켠다 |

### 11-2. 화면 부품 규칙표

| 갈래 | 부품 | 켜지는 조건 | 딸려오는 것 |
|---|---|---|---|
| A | 목록 화면 | 무조건 | 빈 상태 안내 |
| A | 상세 화면 | A1 = 있다 | — |
| A | 입력 폼 | A2 = 직접 등록 | 필수 입력 오류 안내 |
| A | 요약 화면 | A3 = 필요하다 | — |
| A | 검색·필터 | A4 = 그렇다 | 목록 화면에 포함 |
| B | 단일 입력 화면 | B1 = 1~3개 | — |
| B | 단계별 질문 화면 | B1 = 4개 이상 | 진행 표시 |
| B | 결과 화면 | 무조건 | 다시 하기 버튼 |
| B | 기록 목록 화면 | B3 = 필요하다 | — |
| C | 메인 페이지 | 무조건 | — |
| C | 목록 섹션 | C2 = 있다 | 메인 페이지에 포함 |
| C | 하위 페이지 | C1 = 5개 이상 | — |
| C | 문의 폼 | C3 = 필요하다 | — |

**화면 상한 5개.** 초과 시 절단 순서 — A: 목록 → 입력 폼 → 상세 → 요약 / B: 입력 → 결과 → 기록 목록 / C: 메인 → 하위 → 문의 폼

**화면 이름 생성 규칙.** `Q1 입력값 + 부품 기본명`. 예) Q1 = "감귤 출하 기록" → "감귤 출하 기록 목록", "감귤 출하 기록 상세"

### 11-3. 화면 연결 규칙

| 갈래 | 이동 경로 |
|---|---|
| A | 목록 → 항목 클릭 → 상세 / 목록 → 추가 버튼 → 입력 폼 / 입력 완료 → 목록 / 목록 ↔ 요약 (상단 탭) |
| B | 입력 → 결과 / 결과 → 다시 하기 → 입력 / 결과 → 저장 → 기록 목록 / 기록 목록 → 항목 클릭 → 결과 |
| C | 메인 안에서 스크롤 이동 / 메인 → 하위 페이지 / 어디서든 → 문의 폼 |

### 11-4. 예시 카드 프리셋 8개

| 예시 | 갈래 | 생성되는 화면 |
|---|---|---|
| 하루 지출 기록 | A | 목록, 상세, 입력 폼 |
| 개인 독서 기록 | A | 목록, 상세, 입력 폼, 요약 |
| 동아리 회비 관리 | A | 목록, 입력 폼, 요약 |
| 우리 팀 회의록 모음 | A | 목록, 상세, 입력 폼 (+검색) |
| 간단 건강 계산기 | B | 입력 화면, 결과 화면 |
| 성향 진단 테스트 | B | 단계별 질문 화면, 결과 화면 |
| 소규모 가게 소개 | C | 메인 페이지, 문의 폼 |
| 강의 안내 페이지 | C | 메인 페이지, 하위 페이지, 문의 폼 |

### 11-5. 줄글 경로 프롬프트 템플릿 (FN-12)

사용자가 적은 줄글은 **요약하거나 가공하지 않고 원문 그대로** 아래 자리에 끼워 넣는다. 서비스가 더하는 것은 통제 조건뿐이다.

```
아래 요구사항으로 웹앱을 만들어줘.

[만들려는 것]
{사용자 줄글 원문}

[개발 조건]
- Next.js(App Router) + TypeScript + Tailwind CSS
- 서버·DB·로그인 없이 브라우저 저장(localStorage)만 사용
- 한 번에 다 만들지 말고, 먼저 화면 목록과 파일 구조를 제안하고
  내가 확인하면 구현할 것
- 위 요구사항에 없는 기능은 추가하지 말 것.
  필요하다고 판단되면 만들지 말고 나에게 질문할 것
- 무엇을 왜 만들었는지 각 단계마다 한 줄로 설명할 것

[이번 버전에서 만들지 않을 것]
{사용자가 적은 제외 항목. 비어 있으면 기본 목록 사용}
기본 목록: 로그인·회원가입, 결제, 다국어, 실시간 알림, 관리자 승인
```

**입력 보조**

- textarea 위 힌트 3개 — 무엇을 다루는가 / 누가 쓰는가 / 무엇은 안 만들 것인가
- 30자 미만이면 "조금 더 자세히 적으면 결과가 좋아집니다" 안내. 생성 자체는 막지 않는다

### 11-6. 저장 구조 (localStorage)

```
key: vibe-prd-draft
value: {
  q1: string,          // 대상 이름
  q2: 'self' | 'team' | 'guest',
  track: 'A' | 'B' | 'C',
  answers: { [질문ID]: 값 | 'unknown' },
  updatedAt: number
}
```

---

## 12. 위험과 대응

| 위험 | 대응 |
|---|---|
| 초보자가 추상적 질문에 답하지 못하고 이탈 | 분류어 대신 실물 예시로 질문 / 모든 질문에 "잘 모르겠어요" / 예시 카드로 시작 가능 |
| 규칙표가 애매하게 전달되어 구현이 어긋남 | 규칙을 조건표로 빈틈없이 작성해 개발 프롬프트에 그대로 포함 |
| 생성된 프롬프트가 실제로 작동하지 않음 | 배포 전 2건 검증 — 본인 주제 1건, 다른 갈래 1건 |
| 시간 초과로 배포 실패 | 시작 30분 안에 빈 페이지 선(先) 배포 / Could·Won't 항목은 즉시 절단 |
| 세 갈래로 커버되지 않는 유형 유입 | 제외 범위를 첫 화면에 명시 |

---

## 13. Task 분해

| 순서 | 작업 | 완료 기준 | 의존성 |
|---|---|---|---|
| 1 | Next.js 프로젝트 생성 + Vercel 첫 배포 | 빈 페이지가 배포 URL에서 열린다 | — |
| 2 | 질문지 데이터 파일 작성 (질문·선택지·설명·기본값) | 질문 데이터가 코드에 들어가 있다 | 1 |
| 3 | 화면 부품 규칙표를 판정 함수로 구현 | 답변 객체를 넣으면 화면 배열이 나온다 | 2 |
| 4 | 첫 화면 (진입로 3개 + 예시 카드 8개) | 카드 클릭 시 결과로 이동한다 | 3 |
| 5 | 질문 화면 (순차 진행, "잘 모르겠어요" 포함) | 마지막 질문까지 진행된다 | 2 |
| 6 | 결과 화면 (화면 목록 + 연결 + 기본값 안내) | 화면 목록과 이동 경로가 표시된다 | 3, 5 |
| 7 | 개발 프롬프트 생성 + 복사 버튼 | 복사한 텍스트에 답변·화면·연결·제외 범위가 다 들어 있다 | 6 |
| 8 | localStorage 자동 저장 | 새로고침 후 답변이 남아 있다 | 5 |
| 9 | 빠른 입력 화면 (줄글 → 프롬프트) | 줄글을 적고 생성하면 통제 조건이 붙은 프롬프트가 나온다 | 4 |
| 10 | 검증 2건 + 최종 배포 | 생성한 프롬프트로 Claude Code가 오류 없이 첫 화면을 만든다 | 7 |

**AI 기능은 이번 버전에 없다.** 데이터 구조와 기본 동작부터 쌓는 순서를 지킨다.

---

## 부록 A. 수용 기준 전체

**AC-01 (FN-01)**
조건: 질문 화면에서 / 행동: 질문에 순서대로 답한다 / 기대 결과: 미답변 항목이 남아 있으면 결과 생성으로 넘어가지 않는다

**AC-02 (FN-02)**
조건: 질문 화면에서 / 행동: 아무 질문이나 본다 / 기대 결과: 그 질문이 PRD의 어느 항목이고 왜 필요한지가 한 줄로 함께 표시된다

**AC-03 (FN-05)**
조건: 모든 질문에 답한 상태에서 / 행동: 결과 생성을 누른다 / 기대 결과: 화면 목록이 1개 이상 5개 이하로 표시되고, 각 화면 이름에 Q1 입력값이 들어가 있으며, 화면 간 이동 경로가 함께 표시된다

**AC-04 (FN-06)**
조건: 화면 구성안이 생성된 상태에서 / 행동: 프롬프트 영역을 본다 / 기대 결과: 사용자 답변·화면 목록·화면 연결·제외 범위가 모두 포함된 텍스트가 표시된다

**AC-05 (FN-07)**
조건: 프롬프트가 표시된 상태에서 / 행동: 복사 버튼을 누른다 / 기대 결과: 전체 텍스트가 클립보드에 담기고 복사 완료가 화면에 표시된다

**AC-06 (FN-03)**
조건: 아무 질문 화면에서 / 행동: "잘 모르겠어요"를 누른다 / 기대 결과: 다음 질문으로 넘어가고, 결과 화면에 해당 항목이 기본값으로 정해졌다고 표시된다

**AC-07 (FN-04)**
조건: 첫 화면에서 / 행동: 예시 카드를 하나 누른다 / 기대 결과: 그 예시의 화면 구성안과 프롬프트가 즉시 표시되고, "내 걸로 바꾸기"로 답이 채워진 질문 화면에 들어갈 수 있다

**AC-08 (FN-08)**
조건: 질문에 답하던 중 / 행동: 새로고침한다 / 기대 결과: 입력했던 답변이 그대로 남아 있다

**AC-09 (FN-12)**
조건: 첫 화면에서 "이미 정해졌어요"를 누르고 줄글을 적은 상태에서 / 행동: 생성을 누른다 / 기대 결과: 줄글 원문이 손실 없이 포함되고 개발 조건과 제외 범위가 함께 붙은 프롬프트가 표시되며, 복사 버튼으로 전체를 복사할 수 있다

---

## 부록 B. 성공 지표

| 지표 | 목표 |
|---|---|
| 최종 검증 | 생성된 프롬프트를 Claude Code에 붙여넣었을 때 오류 없이 첫 화면이 뜬다 |
| 검증 건수 | 서로 다른 갈래로 2건 이상 |
| 완료 소요 시간 | 첫 화면 진입부터 프롬프트 복사까지 5분 이내 |
| 이해도 | 강의 동기 1명이 사전 설명 없이 끝까지 진행한다 |

---

## 부록 C. 기술 스택

| 구분 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | Vercel 배포가 가장 단순 |
| 언어 | TypeScript | 규칙표를 타입으로 고정 |
| 스타일 | Tailwind CSS | 별도 설정 없이 빠르게 |
| 저장 | localStorage | 서버·DB·인증 불필요 |
| 배포 | Vercel | 강의 실습 환경과 동일 |
| 개발 도구 | VS Code + Claude Code | — |

**외부 API 의존이 없다.** 배포 후 실패할 수 있는 구간이 존재하지 않는다.
