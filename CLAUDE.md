# 프로젝트 지침

바이브코딩 초보자가 질문에 답하면 화면 구성안과 개발 프롬프트를 받는 웹사이트.
상세 명세는 `PRD.md`에 있다. PRD가 유일한 기준이다.

## 반드시 지킬 것

- PRD에 없는 기능은 만들지 않는다. 필요하다고 판단되면 만들지 말고 질문한다.
- 큰 작업은 먼저 파일 목록과 역할을 제안하고, 확인을 받은 뒤 구현한다.
- 규칙 판정 로직은 `src/lib/planner.ts` 안에만 둔다. 화면 컴포넌트에서 판정하지 않는다.
- `PRD.md`의 표에 적힌 조건을 임의로 해석하거나 보완하지 않는다.

## 기술 제약

- Next.js(App Router) + TypeScript + Tailwind CSS
- 서버 라우트, DB, 인증, 외부 API 호출을 만들지 않는다
- 저장은 localStorage만 사용한다
- Tailwind 외의 UI 라이브러리를 설치하지 않는다
- PC 전용. 모바일 반응형 분기를 만들지 않는다

## 디자인 규칙

깔끔하고 조용한 화면. 장식보다 읽기 쉬움을 우선한다.

### 색

| 용도 | 값 |
|---|---|
| 배경 | `#FFFFFF` |
| 본문 글자 | `#1A1A1A` |
| 보조 글자 | `#6B7280` |
| 강조색 | `#2563EB` |
| 경계선 | `#E5E7EB` |
| 연한 배경 (카드·안내) | `#F9FAFB` |

색은 위 6개만 쓴다. 강조색은 하나뿐이며 버튼, 선택된 항목, 진행 표시에만 쓴다.

### 글자

- 폰트: `system-ui, -apple-system, "Segoe UI", sans-serif`
- 본문 16px, 작은 글씨 14px, 화면 제목 28px, 섹션 제목 20px
- 줄간격 1.7
- 굵기는 400과 600 두 가지만 쓴다

### 여백과 형태

- 콘텐츠 최대 너비 720px, 중앙 정렬. 질문 화면은 640px
- 요소 사이 16px 또는 24px, 섹션 사이 64px
- 모서리 반경 8px
- 그림자를 쓰지 않는다. 구분은 경계선 1px과 연한 배경으로 한다
- 아이콘은 꼭 필요한 곳에만. 장식용 아이콘을 넣지 않는다

### 버튼

- 채운 버튼 — 강조색 배경, 흰 글자. 화면당 하나만
- 테두리 버튼 — 흰 배경, 경계선, 검정 글자
- 높이 44px, 좌우 여백 20px

### 하지 않을 것

- 애니메이션, 전환 효과, 호버 확대
- 다크모드
- 그라디언트, 그림자, 둥근 알약형 버튼
- 이모지를 UI 요소로 쓰는 것 (Q3 선택지의 예시 아이콘은 예외)

## 문구 규칙

화면에 보이는 모든 글은 비전공자가 읽는다.

- 개발 용어를 쓰지 않는다: PRD, 스펙, 컴포넌트, CRUD, 라우팅, 상태, 엔티티
- 대신 쓴다: 기획서, 화면, 만들 것, 이동, 저장한 내용
- 갈래 기호(A/B/C)를 화면에 노출하지 않는다. 내부 값으로만 쓴다
- 안내 문구는 한 문장으로 끝낸다
