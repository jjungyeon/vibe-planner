# 프로젝트 지침

바이브코딩 초보자가 만들고 싶은 것을 적고 질문에 답하면, **오른쪽에 화면 레이아웃이 그려지고**, 마지막에 PRD와 개발 프롬프트를 받는 웹사이트.

상세 명세는 `PRD.md`, 개조 지시는 `PROMPTS2.md`에 있다.

## 반드시 지킬 것

- PRD에 없는 기능은 만들지 않는다. 필요하다고 판단되면 만들지 말고 질문한다.
- 큰 작업은 먼저 파일 목록과 역할을 제안하고, 확인을 받은 뒤 구현한다.
- 규칙 판정은 `src/lib/planner.ts` 안에만 둔다. 화면 컴포넌트에서 판정하지 않는다.
- **오른쪽 미리보기는 진짜 앱이 아니다.** 작동하지 않는 레이아웃 그림(와이어프레임)이다. 실제 기능을 넣지 않는다.

## 기술 제약

- Next.js(App Router) + TypeScript + Tailwind CSS
- 서버 라우트, DB, 인증, 외부 API 호출을 만들지 않는다
- 저장은 localStorage만 사용한다
- Tailwind 외의 UI 라이브러리를 설치하지 않는다
- 사이트 자체는 PC 전용. 모바일 반응형 분기를 만들지 않는다
  (미리보기 안의 "모바일 화면"은 별개다. 그건 그림이다)

---

# 디자인 규칙

참고: Klarna 웹사이트. 흰 배경, 크고 굵은 검정 글자, 알약형 버튼, 넉넉한 여백.

## 색

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| 배경 | `#FFFFFF` | 전체 |
| 글자 | `#0B0518` | 제목·본문 |
| 보조 글자 | `#6B6577` | 설명·안내 |
| 강조 (핑크) | `#FFA8CD` | 선택된 항목 표시, 강조 배경 |
| 검정 버튼 | `#0B0518` | 주 버튼 배경 |
| 경계선 | `#E6E3E8` | 카드 테두리, 구분선 |
| 연한 배경 | `#F6F4F2` | 미리보기 바탕, 안내 상자 |

색은 위 7개만 쓴다. 파랑은 쓰지 않는다.

## 글자

- 폰트: `system-ui, -apple-system, "Segoe UI", sans-serif`
- 큰 제목 48px / 굵기 800 / 자간 -0.02em
- 화면 제목 30px / 굵기 700
- 소제목 20px / 굵기 700
- 본문 17px / 굵기 400 / 줄간격 1.6
- 작은 글씨 14px / 색 `#6B6577`

**제목은 크고 굵게 쓴다.** 이 디자인의 인상은 제목 크기에서 나온다.

## 모양

- 버튼 — 알약형 `border-radius: 999px`, 높이 52px, 좌우 여백 28px
  - 주 버튼: 검정 배경 + 흰 글자
  - 보조 버튼: 흰 배경 + 경계선 + 검정 글자
- 카드 — `border-radius: 24px`, 경계선 1px, 흰 배경, 안쪽 여백 28px
- 입력칸 — `border-radius: 16px`, 경계선 1px, 높이 52px
- 큰 입력칸(줄글) — `border-radius: 24px`, 최소 높이 200px

## 여백

- 요소 사이 16px 또는 24px
- 섹션 사이 80px
- 페이지 좌우 여백 최소 40px
- 첫 화면 콘텐츠 최대 너비 880px

## 하지 않을 것

- 그림자, 그라디언트
- 애니메이션 (미리보기에 새 요소가 추가될 때 짧게 강조되는 것만 예외)
- 다크모드
- 장식용 아이콘

---

# 화면 구조 규칙 (2분할)

참고: Lovable. 왼쪽은 대화·질문, 오른쪽은 미리보기.

```
┌──────────────────┬─────────────────────────┐
│                  │ [PC][모바일]  화면▾  ↻   │  ← 미리보기 툴바
│   질문 패널       ├─────────────────────────┤
│   (너비 40%)      │                         │
│                  │      미리보기 프레임      │
│                  │      (연한 배경 위)       │
│                  │                         │
└──────────────────┴─────────────────────────┘
```

- 왼쪽 40%, 오른쪽 60%. 사이에 세로 구분선 1px
- 왼쪽은 위에서부터 쌓이고, 아래에 이전·다음 버튼
- 오른쪽 미리보기 프레임
  - PC 모드: 가로로 넓은 사각형 (16:10 비율), 흰 배경, 라운드 16px, 경계선
  - 모바일 모드: 세로로 긴 사각형 (390×760 비율), 흰 배경, 라운드 32px, 경계선
  - 프레임 바깥은 연한 배경 `#F6F4F2`
- 화면이 2개 이상이면 툴바의 드롭다운으로 전환

---

# 문구 규칙

화면에 보이는 모든 글은 비전공자가 읽는다.

- 개발 용어를 쓰지 않는다: PRD, 스펙, 컴포넌트, CRUD, 라우팅, 와이어프레임, 상태
- 대신 쓴다: 기획서, 화면, 만들 것, 이동, 화면 그림
- 갈래 기호(A/B/C)를 화면에 노출하지 않는다
- 안내 문구는 한 문장으로 끝낸다
