# Claude Code 개발 프롬프트 (3단계)

> 프로젝트 임시명: `vibe-planner`
> 전제: VS Code에서 빈 폴더를 열고, 그 안에 `PRD.md`(앞서 만든 PRD 문서)를 넣어둔 상태에서 시작
> 사용법: 아래 프롬프트를 **순서대로 하나씩** 붙여넣는다. 한 번에 다 넣지 않는다.

---

## 사전 준비 (프롬프트 아님, 직접 할 것)

1. 빈 폴더를 만든다 → 폴더명 `vibe-planner`
2. 그 폴더에 PRD 문서를 `PRD.md` 이름으로 넣는다
3. VS Code로 그 폴더를 연다
4. 터미널에서 `claude` 실행

---

# 프롬프트 1 — 셋업과 첫 배포

```
이 폴더에 있는 PRD.md를 먼저 읽어줘. 앞으로 이 문서가 유일한 기준이다.
PRD에 없는 기능은 추가하지 말고, PRD와 다르게 만들고 싶은 부분이 있으면
구현하지 말고 나에게 먼저 질문해줘.

지금은 1단계다. 프로젝트 뼈대만 만든다.

할 것:
1. Next.js(App Router) + TypeScript + Tailwind CSS 프로젝트를 이 폴더에 생성
2. 앱 라우터 구조만 잡기 — 페이지 내용은 "준비 중" 한 줄이면 충분
3. .gitignore 확인
4. git 초기화 후 첫 커밋

하지 말 것:
- 질문 데이터, 규칙 엔진, 화면 구현은 아직 만들지 않는다
- 외부 라이브러리를 추가로 설치하지 않는다 (Tailwind 외)
- 상태관리 라이브러리, DB, 인증, API 라우트를 만들지 않는다

작업 전에 실행할 명령어 목록을 먼저 보여주고, 내가 확인하면 진행해줘.
```

**이 프롬프트가 끝나면 직접 할 것**

1. GitHub에 저장소를 만들고 push
2. vercel.com에서 그 저장소를 import → Deploy
3. 배포 URL이 열리는지 확인 — **여기까지 되어야 다음으로 넘어간다**

---

# 프롬프트 2 — 질문 데이터와 규칙 엔진

```
2단계다. 화면은 아직 만들지 않는다. 데이터와 판정 로직만 만든다.

PRD.md의 "11. 데이터 구조" 섹션을 그대로 코드로 옮겨줘.
표에 적힌 조건을 임의로 해석하거나 보완하지 말고, 적힌 그대로 구현한다.
표에 없는 조건이 필요하다고 판단되면 만들지 말고 나에게 질문해줘.

만들 파일:

1. src/data/questions.ts
   - PRD 11-1의 질문지 전체 (공통 Q1~Q3, A갈래 A1~A4, B갈래 B1~B3, C갈래 C1~C3)
   - 각 질문에 담을 것: id, 질문 문구, 선택지 배열, PRD 항목명, 옆에 붙는 설명 한 줄,
     "잘 모르겠어요" 선택 시 적용할 기본값
   - 화면에 A/B/C 같은 분류 기호를 노출하지 않는다 (PRD의 NF-03).
     Q3 선택지 문구는 "가계부처럼 / BMI 계산기처럼 / 가게 소개 페이지처럼"으로 쓰고
     내부 값만 'A' | 'B' | 'C'로 둔다

2. src/data/presets.ts
   - PRD 11-4의 예시 카드 8개
   - 각 카드는 질문 답변 값만 갖는다. 결과 화면 데이터는 갖지 않는다

3. src/lib/planner.ts
   - 입력: 답변 객체 / 출력: { screens: 화면배열, links: 연결배열, defaultsUsed: 기본값적용항목배열 }
   - PRD 11-2 규칙표대로 화면 부품을 켜고 끈다
   - 화면 이름은 "Q1 입력값 + 부품 기본명"으로 만든다 (예: "하루 지출 기록 목록")
   - 화면 상한 5개. 초과 시 PRD에 적힌 갈래별 절단 순서를 적용한다
   - "잘 모르겠어요"로 답한 항목은 기본값을 적용하고, 어떤 항목이 기본값이었는지
     defaultsUsed에 담는다
   - PRD 11-3의 연결 규칙대로 links를 만든다

4. src/lib/buildPrompt.ts
   - 입력: 답변 객체 + planner 결과 / 출력: 개발 프롬프트 문자열 하나
   - 프롬프트에 반드시 포함할 것: 만들려는 것 설명, 사용자, 화면 목록,
     화면 간 연결, 각 화면에 들어갈 요소, 제외 범위, "이 목록에 없는 기능은
     추가하지 말 것"이라는 문장

5. src/lib/buildFreePrompt.ts
   - PRD 11-5의 줄글 경로 템플릿을 그대로 구현한다
   - 입력: 사용자 줄글 + 제외 항목 문자열 / 출력: 프롬프트 문자열
   - 줄글은 요약·가공·재작성하지 않고 원문 그대로 템플릿에 끼워 넣는다
   - 갈래 판정이나 화면 계산을 하지 않는다. planner를 호출하지 않는다
   - 제외 항목이 비어 있으면 PRD에 적힌 기본 목록을 쓴다

마지막으로, 예시 카드 8개를 planner에 통과시킨 결과를 표로 출력해줘.
PRD 11-4의 "생성되는 화면" 열과 일치하는지 내가 눈으로 확인하겠다.
```

**확인 포인트** — 출력된 표가 PRD 11-4와 다르면 규칙이 어긋난 것이다. 화면 구현으로 넘어가기 전에 여기서 잡는다.

---

# 프롬프트 3 — 화면 구현

```
3단계다. 화면을 만든다. 2단계에서 만든 데이터와 함수만 사용하고,
판정 로직을 화면 안에서 새로 작성하지 않는다.

PC 전용이다. 모바일 반응형은 만들지 않는다 (PRD 제외 범위).

만들 화면 4개:

1. 첫 화면 (/)
   - 서비스 한 줄 설명
   - 진입 버튼 3개:
     "만들고 싶은 게 있어요" / "이미 정해졌어요, 바로 쓸게요" / "예시부터 볼래요"
   - "예시부터 볼래요"를 누르면 예시 카드 8개가 펼쳐진다
   - 카드를 누르면 그 프리셋으로 결과 화면에 바로 간다

2. 질문 화면 (/plan)
   - 질문을 한 번에 하나씩 보여준다. 진행 표시(3/7 형태)를 넣는다
   - 각 질문 아래에 PRD 항목명과 설명 한 줄을 항상 보여준다
   - 모든 질문에 "잘 모르겠어요" 선택지를 넣는다
   - Q3 답변에 따라 이후 질문이 갈래별로 분기한다
   - 답변은 변경될 때마다 localStorage 키 'vibe-prd-draft'에 저장한다.
     페이지에 다시 들어오면 저장된 답변을 복원한다
   - 마지막 질문에 답하면 결과 화면으로 이동한다

3. 결과 화면 (/result)
   - 화면 목록 — 각 화면 이름과 그 화면에 들어갈 요소
   - 화면 연결 — "A 화면에서 X를 누르면 B 화면으로" 형태의 문장 목록
   - 기본값 안내 — defaultsUsed가 비어있지 않으면
     "이 항목은 기본값으로 정했습니다. 나중에 바꿔도 됩니다" 안내와 항목 목록
   - 개발 프롬프트 — buildPrompt 결과를 그대로 보여준다
   - 복사 버튼 — 누르면 프롬프트 전체가 클립보드에 담기고 "복사됨" 표시가 뜬다
   - 예시로 들어온 경우 "내 걸로 바꾸기" 버튼 — 누르면 그 답변이 채워진 채로 질문 화면으로 간다

4. 빠른 입력 화면 (/quick)
   - 한 화면에 전부 펼쳐 놓는다. 질문을 하나씩 넘기지 않는다
   - 큰 textarea 하나 — "만들려는 것을 자유롭게 적어주세요"
   - textarea 위에 힌트 3개를 항상 보여준다:
     "무엇을 다루는가 / 누가 쓰는가 / 무엇은 안 만들 것인가"
   - 작은 입력란 하나 — "안 만들 것이 있다면 적어주세요" (선택)
   - 30자 미만이면 "조금 더 자세히 적으면 결과가 좋아집니다"를 안내로 띄운다.
     생성 자체는 막지 않는다
   - 생성 버튼 → buildFreePrompt 결과를 같은 화면 아래에 표시 + 복사 버튼
   - 이 화면은 화면 목록이나 화면 연결을 보여주지 않는다.
     planner를 호출하지 않는다

디자인:
- 흰 배경, 검정 글자, 강조색 1개만 사용
- 글자 크기를 넉넉히. 비전공자가 읽는 화면이다
- 개발 용어(PRD, 스펙, CRUD, 컴포넌트 등)를 화면에 쓰지 않는다.
  "기획서", "화면", "만들 것" 같은 일상어로 쓴다

만들지 말 것:
- 로그인, 서버 저장, AI 호출, 코드 생성, 화면 도식, 파일 다운로드
- 다크모드, 애니메이션, 모바일 레이아웃

먼저 파일 목록과 각 파일이 맡을 역할을 보여주고, 내가 확인하면 구현해줘.
```

---

# 프롬프트 4 (예비) — 마무리와 재배포

```
PRD.md의 "부록 A. 수용 기준" 8개를 하나씩 확인해줘.
각 기준마다 통과 / 미통과를 표로 표시하고, 미통과인 것만 고쳐줘.
새 기능은 추가하지 않는다.

확인이 끝나면 빌드가 통과하는지 확인하고 커밋해줘.
```

---

# 시간 배분

| 시각 | 작업 |
|---|---|
| 0:00–0:30 | 사전 준비 + 프롬프트 1 + GitHub·Vercel 첫 배포 |
| 0:30–1:15 | 프롬프트 2 + 예시 8개 결과 검증 |
| 1:15–2:45 | 프롬프트 3 + 화면 손보기 |
| 2:45–3:05 | 프롬프트 4 + 재배포 |
| 3:05–3:30 | 생성 프롬프트로 실제 검증 2건 + 여유 |

---

# 막혔을 때 쓰는 문장

| 상황 | 붙여넣을 말 |
|---|---|
| 안 시킨 걸 만들었을 때 | `PRD에 없는 기능이다. 방금 추가한 것을 되돌려줘.` |
| 이전 작업이 망가졌을 때 | `방금 수정에서 이전에 동작하던 OO이 깨졌다. 그 부분만 원래대로 돌려줘.` |
| 한 부분만 고치고 싶을 때 | `OO 파일의 OO 함수만 수정해줘. 다른 파일은 건드리지 마.` |
| 오류가 났을 때 | `이 오류가 났다. 원인을 먼저 설명하고, 고칠 방법을 제안해줘. 아직 고치지는 마.` |
| 시간이 부족할 때 | `지금 상태에서 빌드가 통과하고 배포되는 것만 우선으로 처리해줘. 나머지는 남겨둬.` |
