# 개조 프롬프트 (5단계)

> 지금 만들어진 것을 살리면서 화면 구조만 바꾼다.
> 순서대로 하나씩 붙여넣는다.

---

## 시작 전에 직접 할 것

1. `CLAUDE_v2.md` 파일 내용으로 폴더의 `CLAUDE.md`를 **덮어쓴다**
2. 이 파일을 폴더에 `PROMPTS2.md`로 저장한다
3. Claude Code에서 **프롬프트 0**부터 시작

---

# 프롬프트 0 — 지금 상태 저장

```
지금까지 만든 것을 git push 해줘.
Vercel이 자동으로 배포할 거다.

그리고 지금 커밋에 태그를 붙여줘. 이름은 v1-text 로 한다.
앞으로 큰 개조를 할 건데, 잘못되면 여기로 돌아올 자리가 필요하다.
```

**push가 끝나면 배포 주소에 들어가서 화면이 뜨는지 확인한 뒤 다음으로 넘어간다.**

---

# 프롬프트 1 — 무엇을 바꾸는지 알리기

```
CLAUDE.md가 바뀌었다. 다시 읽어줘. 그리고 PROMPTS2.md도 읽어줘.

지금부터 큰 개조를 한다. 무엇이 바뀌는지 먼저 정리한다.

[살리는 것 — 손대지 않는다]
- src/data/questions.ts (질문 목록)
- src/data/presets.ts (예시 8개)
- src/lib/planner.ts (답 → 화면 판정)
- src/lib/buildPrompt.ts, buildFreePrompt.ts (프롬프트 생성)
- src/lib/storage.ts (저장)

[바꾸는 것]
- 첫 화면 : 버튼 3개 → 큰 줄글 입력칸 하나
- 질문 화면 : 한 화면 꽉 채움 → 왼쪽 질문 / 오른쪽 화면 미리보기 2분할
- 미리보기 : 없음 → 답할 때마다 화면 레이아웃 그림이 채워짐
- 질문 : PC냐 모바일이냐 묻는 질문 추가
- 결과 화면 : 글 목록 → 기획서 전문 + 개발 프롬프트

[새로 만드는 것]
- 화면 레이아웃 그림을 그리는 부품 모음

이 개조의 핵심은 오른쪽 미리보기다.
사용자가 질문에 답할 때마다 오른쪽 화면 그림에 요소가 하나씩 늘어나야 한다.
이게 이 서비스가 파는 것이다.

미리보기는 진짜 앱이 아니다. 작동하지 않는 레이아웃 그림이다.
버튼을 눌러도 아무 일이 일어나지 않는다. 회색 네모와 선으로 형태만 보여준다.

이해했으면 "확인"만 답하고 기다려라. 아직 아무것도 만들지 마라.
```

---

# 프롬프트 2 — 질문 재배치와 PC/모바일 추가

```
2단계다. 질문 목록만 손본다. 화면은 아직 건드리지 마라.

1) 질문 순서를 이렇게 바꾼다.

  Q0  (첫 화면에서 받는 줄글. 질문 목록에는 없다)
  Q1  무엇을 다루는 서비스인가요?        자유 입력
  Q2  누가 쓰나요?                      나만 / 우리 팀·가족 / 손님·외부인
  Q3  만들려는 것과 가장 비슷한 걸 골라주세요
       가계부처럼 — 뭔가를 계속 적어서 쌓아둡니다
       BMI 계산기처럼 — 입력하면 답이 나옵니다
       가게 소개 페이지처럼 — 정해진 내용을 보여줍니다
  Q4  어디서 쓸 화면인가요?   ← 새로 추가
       컴퓨터 화면 / 휴대폰 화면 / 둘 다
  그 다음 갈래별 후속 질문 (기존 A1~A4 / B1~B3 / C1~C3 그대로)

2) Q4를 questions.ts에 추가한다.
   - id: 'device'
   - 값: 'pc' | 'mobile' | 'both'
   - "잘 모르겠어요" 있음. 기본값은 'pc'
   - PRD 항목명: 비기능 요구사항
   - 설명 한 줄: 어디서 보느냐에 따라 화면 생김새가 달라집니다
   - '둘 다'를 고르면 미리보기에서 컴퓨터·휴대폰을 번갈아 볼 수 있다

3) 저장 구조에 device 를 추가한다. 기존 키 이름은 그대로 둔다.

4) planner.ts 의 화면 판정 결과는 바뀌지 않는다.
   device 는 화면 개수를 바꾸지 않고 생김새만 바꾼다.
   planner 를 고치지 마라.

5) buildPrompt.ts 가 만드는 프롬프트에 한 줄 추가한다.
   "이 앱은 (컴퓨터 / 휴대폰 / 컴퓨터와 휴대폰 모두) 화면을 기준으로 만든다"

파일 목록을 먼저 보여주고, 확인하면 구현해줘.
```

---

# 프롬프트 3 — 화면 레이아웃 그림 부품 만들기

```
3단계다. 오른쪽 미리보기에 그릴 레이아웃 부품을 만든다.
아직 2분할 화면은 만들지 마라. 부품과 규칙표만 만든다.

새 파일: src/lib/wireframe.ts, src/components/Wireframe.tsx

[규칙]
- 진짜로 작동하지 않는다. 회색 네모, 선, 짧은 글자로 형태만 보여준다
- 글자는 실제 문장 대신 회색 막대로 표현한다. 단, 제목 자리에는
  사용자가 Q1에 적은 이름을 실제로 넣는다
- 색은 CLAUDE.md의 색만 쓴다. 회색 자리표시는 #E6E3E8, 바탕은 #F6F4F2

[블록 종류] — 이것만 쓴다
  topbar      상단 가로 바. 왼쪽에 이름, 오른쪽에 메뉴 3개 자리
  tabbar      하단 탭 4칸 (휴대폰 화면에서만)
  title       큰 제목 한 줄
  searchbar   검색칸 하나
  listcard    카드 (제목 줄 + 설명 줄 2개)
  listrow     한 줄 목록 (왼쪽 글자, 오른쪽 작은 글자)
  fab         오른쪽 아래 동그란 + 버튼
  field       라벨 + 입력칸
  buttonMain  알약형 검정 버튼
  buttonSub   알약형 테두리 버튼
  statcard    큰 숫자 + 작은 설명
  chart       막대 그래프 자리 (막대 5개)
  hero        큰 제목 + 설명 두 줄 + 이미지 자리
  section     소제목 + 본문 두 줄
  resultBig   큰 결과 숫자 상자
  textBlock   문단 자리 (회색 줄 3개)
  progress    진행 막대
  choice      선택지 버튼 3개

[화면별 구성] — 컴퓨터 화면 / 휴대폰 화면

목록 화면
  PC     topbar / title / searchbar(A4=그렇다일 때) / listcard 3개를 3열로 / buttonMain "추가"
  모바일  title / searchbar(A4) / listrow 4개 / fab / tabbar

상세 화면
  PC     topbar / title / hero / field 4개(읽기 전용 모양) / buttonSub 2개
  모바일  title / 이미지 자리 / listrow 4개 / buttonMain / tabbar

입력 폼
  PC     topbar / title / field 4개 / buttonMain "저장"
  모바일  title / field 4개 / buttonMain "저장" / tabbar

요약 화면
  PC     topbar / title / statcard 3개를 가로로 / chart
  모바일  title / statcard 2개 / chart / tabbar

단일 입력 화면
  PC     topbar / title / field 3개 / buttonMain "결과 보기"
  모바일  title / field 3개 / buttonMain / tabbar

단계별 질문 화면
  PC     topbar / progress / title / choice / buttonSub "이전" + buttonMain "다음"
  모바일  progress / title / choice / buttonMain "다음" / tabbar

결과 화면
  PC     topbar / resultBig / textBlock(B2=붙는다일 때) / buttonSub "다시 하기"
  모바일  resultBig / textBlock(B2) / buttonMain "다시 하기" / tabbar

기록 목록 화면
  PC     topbar / title / listrow 5개
  모바일  title / listrow 5개 / tabbar

메인 페이지
  PC     topbar / hero / section 3개 / listcard 3개(C2=있다일 때) / 하단 회색 바
  모바일  간단한 topbar / hero / section 3개 / listcard 2개(C2) / 하단 회색 바

하위 페이지
  PC     topbar / title / textBlock 2개 / 하단 회색 바
  모바일  title / textBlock 2개 / 하단 회색 바

문의 폼
  PC     topbar / title / field 3개 / buttonMain "보내기"
  모바일  title / field 3개 / buttonMain "보내기" / tabbar

[함수]
  buildWireframe(screenId, answers, device) → 블록 배열
  - planner 가 내놓은 화면 목록의 각 화면에 대해 블록 배열을 만든다
  - device 가 'both' 이면 'pc' 기준으로 만들고, 화면에서 전환할 수 있게 한다

마지막에, 예시 카드 8개 각각에 대해
"화면 이름 / PC 블록 개수 / 모바일 블록 개수" 표를 출력해줘.
빈 화면이 하나도 없는지 내가 확인하겠다.

파일 목록을 먼저 보여주고, 확인하면 구현해줘.
```

---

# 프롬프트 4 — 2분할 화면 만들기

```
4단계다. 질문 화면을 2분할로 바꾼다.

주소는 /questions 를 그대로 쓴다. 새 주소를 만들지 마라.

[왼쪽 — 너비 40%]
- 위: 진행 표시 (3/8 형태)
- 가운데: 질문 하나. 그 아래 선택지. 그 아래 회색 작은 글씨로 설명 한 줄
- 아래: "이전" 보조 버튼 + "다음" 주 버튼
- 첫 질문에서는 "이전" 버튼을 숨긴다

[오른쪽 — 너비 60%]
- 위 툴바 (왼쪽부터)
  · 컴퓨터 / 휴대폰 전환 버튼 2개. Q4 답에 따라 처음 상태가 정해진다
    Q4가 '둘 다'가 아니면 고른 쪽만 켜지고 다른 쪽은 흐리게 둔다
  · 화면 선택 드롭다운. 화면이 2개 이상일 때만 보인다
- 아래: 미리보기 프레임. CLAUDE.md의 프레임 규칙대로

[미리보기가 채워지는 순서]
- Q3(비슷한 것)에 답하기 전 : 빈 프레임 + 가운데에 회색 글씨로
  "질문에 답하면 화면이 그려집니다"
- Q3에 답한 순간 : 그 갈래의 기본 화면이 뼈대만 나타난다
- Q4에 답한 순간 : 프레임 모양이 컴퓨터/휴대폰으로 바뀐다
- 후속 질문에 답할 때마다 : 화면이 늘거나 블록이 추가된다
  · 새로 생긴 화면이나 블록에 0.6초 동안 핑크 테두리를 준다
  · 화면이 늘면 드롭다운이 새 화면으로 자동 전환된다

[왼쪽에 한 줄 더]
- 미리보기가 바뀔 때마다 질문 위에 작은 글씨로 무엇이 생겼는지 알린다
  예) "상세 화면이 추가되었습니다"

[하지 말 것]
- planner.ts, questions.ts, presets.ts, buildPrompt.ts 를 고치지 마라
- 미리보기 안의 요소를 클릭해도 아무 일이 없어야 한다
- 미리보기 안에 진짜 입력칸이나 진짜 버튼을 만들지 마라

파일 목록을 먼저 보여주고, 확인하면 구현해줘.
```

---

# 프롬프트 5 — 첫 화면과 결과 화면

```
5단계다. 첫 화면과 결과 화면을 바꾼다.

[첫 화면 /]
- 가운데 정렬, 최대 너비 880px
- 큰 제목 (48px, 굵기 800) 한 줄
  "만들고 싶은 걸 적으면, 화면부터 그려 드립니다"
- 그 아래 설명 한 줄 (17px, 회색)
  "코드는 몰라도 됩니다. 무엇을 만들지만 적어주세요."
- 큰 줄글 입력칸 (최소 높이 200px, 라운드 24px)
  안내 글씨: "예) 우리 동아리 회비를 기록하고 누가 냈는지 확인하는 앱을 만들고 싶어요"
- 입력칸 위에 작은 회색 글씨로 힌트 한 줄
  "무엇을 다루는지 / 누가 쓰는지 / 무엇은 안 만들 것인지를 적으면 좋습니다"
- 그 아래 주 버튼 "시작하기" → /questions 로 이동
- 줄글이 한 글자도 없으면 버튼을 끈다
- 줄글은 저장한다. 다시 들어오면 남아 있어야 한다
- 버튼 아래에 보조 버튼 하나 "예시부터 볼래요" → 기존 예시 카드 8개를 펼친다
- 맨 아래 작은 회색 글씨
  "게임, 실시간 채팅, 지도, 파일 올리기는 다루지 않습니다"

[기존 /quick 주소]
- 첫 화면이 이미 줄글 입력이므로 /quick 은 필요 없어졌다
- /quick 으로 들어오면 / 로 보낸다. 파일은 지우지 마라

[결과 화면 /result]
세 부분으로 나눈다. 위에서부터 순서대로.

1. 내가 만들 것
   - 화면 목록. 각 화면마다 그 화면의 레이아웃 그림을 작게 보여준다
   - 화면 이동 경로를 문장으로
   - 기본값으로 정해진 항목이 있으면 안내

2. 기획서
   - 사용자가 답한 내용을 항목별로 정리해서 보여준다
     무엇을 만드나 / 누가 쓰나 / 어디서 쓰나 / 왜 만드나(첫 화면 줄글) /
     필요한 화면 / 화면 이동 / 이번에 안 만드는 것
   - 이 부분도 복사 버튼을 붙인다

3. 개발 프롬프트
   - buildPrompt.ts 결과를 그대로
   - 복사 버튼
   - 그 아래 회색 작은 글씨로 안내
     "이 글을 복사해서 Claude Code나 Cursor에 붙여넣으면 됩니다"

[첫 화면 줄글을 프롬프트에 넣는다]
- buildPrompt.ts 를 이번에만 고친다
- 프롬프트 맨 앞 "[만들려는 것]" 자리에 첫 화면 줄글 원문을 그대로 넣는다
- 줄글을 요약하거나 고쳐 쓰지 마라

파일 목록을 먼저 보여주고, 확인하면 구현해줘.
```

---

# 프롬프트 6 — 마무리

```
6단계다. 마무리한다.

1) 화면에 개발 용어가 남아 있는지 전부 확인하고 일상어로 바꿔라.
   금지: PRD, 스펙, 컴포넌트, CRUD, 라우팅, 와이어프레임, 렌더링, 상태
2) CLAUDE.md의 색·글자 크기·버튼 모양이 모든 화면에 지켜졌는지 확인해라.
3) 빌드가 통과하는지 확인하고 커밋한 뒤 push 해라.

그리고 아래 두 가지를 직접 확인할 수 있게 정리해줘.
- 예시 카드 8개 각각의 화면 목록이 개조 전과 같은지
- 각 화면에 블록이 하나 이상 그려지는지
```

---

# 시간 배분 (밤 작업 기준)

| 순서 | 할 일 | 시간 |
|---|---|---|
| 0 | 지금 상태 push + 태그 | 10분 |
| 1 | 개조 내용 알리기 | 5분 |
| 2 | 질문 재배치 + PC/모바일 | 20분 |
| 3 | 레이아웃 부품 만들기 | 40분 |
| 4 | 2분할 화면 | 50분 |
| 5 | 첫 화면 + 결과 화면 | 40분 |
| 6 | 마무리 + 배포 | 20분 |

**3번과 4번이 이 개조의 전부다.** 밀리면 5번의 결과 화면을 줄이고 3·4번을 지킨다.

---

# 막혔을 때

| 상황 | 붙여넣을 말 |
|---|---|
| 미리보기가 안 나옴 | `오른쪽 미리보기가 비어 있다. buildWireframe이 무엇을 반환하는지 먼저 확인해줘.` |
| 이전에 되던 게 깨짐 | `방금 수정에서 OO이 깨졌다. 그 부분만 원래대로 돌려줘.` |
| 시키지 않은 걸 만듦 | `PRD와 PROMPTS2.md에 없는 기능이다. 되돌려줘.` |
| 완전히 망가짐 | `v1-text 태그 상태로 되돌려줘.` |
| 시간이 부족 | `지금 상태에서 빌드가 통과하고 배포되는 것만 우선 처리해줘.` |
